# FINDINGS — surfaced to Stephen, never fixed silently

Source of F1–F4: COS working paper `LAB-BRIEF.md` (2026-09-10, scratchpad; sha256 `f031201cc35a3651da7b45f67e485c0995ab9f04f066e6274d258ef9fa621cb4`), section 5.
Source of F5–F15: the build spec and the runtime probe of 2026-09-10 (every path re-read in the WSL checkout at HEAD `d8bed98`).
Nothing below is patched in `src/`, `alembic/`, `frontend/`, `fulfillment/` or `contracts/` by the lab. Railway and Vercel are Stephen's hands.
twin-bfms is a label, not a claim (DEC-009 section 9.8).

## Production and repo state

- F1. `api.muni-pal.io` serves `munipal.main:app` (full BFMS behind auth; `/openapi.json` public). `railway.toml:2` `startCommand = "alembic upgrade head && uvicorn munipal.main:app …"`. Docs and fence say `sensing_app`: `docs/architecture/SENSING_PILOT_FUNNEL.md:84-86` ("Do not deploy munipal.main for the public sensing site"), `tests/unit/test_public_sensing_deployment_boundary.py`, `tests/unit/test_sensing_pilot_funnel_contract.py`. Railway = Stephen's hands. Ruling asked as D2 in `governance/decision-request.md`.
- F2. Two pre-existing red tests on master (520 passed, 2 failed, WSL-native, 2026-09-10): (1) `tests/contract/test_openapi_contract.py::test_openapi_contract_snapshot_is_current` — snapshot drift (obligation-register routes added 9/10 without `python scripts/generate_openapi_snapshot.py`); (2) `tests/unit/test_sensing_pilot_funnel_contract.py::test_sensing_deployment_scope_blocks_bfms_admin_routes` — `sensing_app` mounts `/api/v1/sensing/obligation-register/{intake,privacy}`, which are not in `allowed_public_routes` (`src/munipal/services/sensing_pilot_funnel.py`). Not fixed silently. New lab tests add no red.
- F3. The old OneDrive synthetic packs (the `000_*` … `011_*` entities, 24 in all) are not golden: `C:\Users\st3ja\muni-twin\QUARANTINE.md` — "generation lineage unproven. Audit before blessing." Their manifests carry real firm names (collision failures). The lab generates its own packs (`lab/twin-bfms/packs/`) and borrows the document-type list only.
- F4. The section 5 end-to-end protocol in `docs/pilot/pilot-navigation-system.md` (WP1–WP6 checklists, lines ~437–560; status PARTIAL) was never automated and never run. The lab makes it a test: `tests/integration/test_lab_e2e_housing.py`.

## BFMS behaviour observed by the lab

- F5. Housing vocabulary is split across four files and does not agree: `src/munipal/services/sector_archetypes.py:112-127`, `src/munipal/services/sector_playbooks.py:395-415`, `src/munipal/services/readiness_service.py:124-166`, `src/munipal/services/playbook_data.py` P3.3 (`capital.equity-contribution` hyphen vs `capital.equity_contribution`; `capital.equity-percent` never supplied), and `scripts/seed_demo_scenarios.py`. Both `housing.affordability_restrictions` and `housing.affordability.restrictions` exist. The lab emits both spellings on purpose and records the divergence under `vocabulary_divergence` in every report.
- F6. The MSRB platform token appears outside the CDA Rule 15c2-12 quote in the sellable demo: `fulfillment/demo/approved-inputs.csv` (OB-01 obligation text; recipients OB-01..05), `fulfillment/demo/vault/annual-report-FY2025.txt:3`, `fulfillment/demo/build_fixtures.py:176-184,228`, `fulfillment/demo/output/register.csv`, `fulfillment/demo/output/calendar.csv`, `fulfillment/demo/README.md`. DEC-008 says zero references. The lab scrubs at re-emission (`labkit/generate_pack.py`) and pins the one allowed occurrence (`labkit/law.py::EMMA_ALLOWLIST`). The demo itself is Stephen's call.
- F7. `src/munipal/services/pilot_onboarding.py:189` display_name "Synthetic Oakport end-to-end test passed" (src rename for Stephen).
- F8. `ChecklistService` iterates the UCS `CHECKLIST_ITEMS` constant (`src/munipal/services/checklist_service.py:29,76,105`) and never reads `Playbook.checklist_items`; `ReadinessService` uses `READINESS_CONFIG` + `SECTOR_READINESS_PROFILES` selected by `project.subsector` (`src/munipal/services/readiness_service.py:187,198-199`). No fixture can make P1–P6 sector-true for housing; the lab reports the items whose required paths never intersect the housing intake as `not_applicable` (mechanical set-intersection).
- F9. No artifact-level hash or dedup: re-uploading the same bytes creates a second artifact id; `ArtifactRead` carries no artifact-level sha256 (chunk `content_hash` only). `docs/architecture/BFMS_DATA_MODEL_PRIMITIVES.md` names vault reuse and hashing as a required mechanism (`pilot_onboarding.py::_REQUIRED_MECHANISMS`, `artifact_vault_reuse_and_hashing`). Recorded as an expected `fail` in every golden (`S05.artifact_dedup`, `S05.artifact_level_sha256`).
- F10. The sellable demo's scenario is not valid deal-v0: `fulfillment/demo/scenario/syn-hsg-2025.synthetic.json` has no `stations`; `APPLIED RESEARCH/bondi-bfms/synth-issuance/schema/deal-v0.json` requires it. The lab adds `stations` and compares only the shared core (`tests/unit/test_lab_scenario.py`).
- F11. Two route defects: (a) `GET /api/v1/playbooks/{id}` returns 500 — `PlaybookDetail.schema_paths[*].description` is required (`src/munipal/core/schemas/playbook.py:26`) but every `playbook_data.SCHEMA_PATHS` dict omits it (90 validation errors); (b) `POST /api/v1/deal-documents/{id}/status` raises `AttributeError: 'str' object has no attribute 'value'` at `src/munipal/api/routes/deal_documents.py:233` because `BaseSchema` sets `use_enum_values=True` (`src/munipal/core/schemas/base.py:21`). The lab never calls (a) and calls `DealDocumentService.transition_status` directly for (b).
- F12. `housing.*` paths are untyped: absent from `_SCHEMA_BY_PATH` (`src/munipal/services/extraction/rules_based_extractor.py:33-50`), so every housing value is a string and numeric coercion is lost; the extraction fallback ignores the project's sector and unions the UCS extractor targets when `target_schema_paths` is omitted (`src/munipal/api/routes/extraction.py:168-180`). The lab always passes explicit targets and compares as strings (int only for `capital.project-cost`).
- F13. `scripts/seed_demo_scenarios.py` inserts approved facts with no chunks (bypasses Artifact→Chunk→Page provenance). Unusable as a lab fixture; the lab uploads real bytes and extracts.
- F14. Alembic seeds: `alembic/versions/20260226_0001_*` carries the MSRB platform token in `deal_document_types` descriptions (POS/OS rows); `alembic/versions/20260127_0002_*_seed_ucs_playbook.py` is Postgres-only (`NOW()`). `Base.metadata.create_all` leaves `deal_document_types` empty, so the lab seeds one `DealDocumentType` with its own wording and never imports `alembic`.
- F15. `src/munipal/services/sensing.py:33-53` auto-discovers the retired extractor's SQLite corpus under `emma/` in the WSL checkout (`_find_munipal_root` step 2 and step 4) unless `MUNIPAL_ROOT` is set before import. That corpus is Class D (`C:\Users\st3ja\muni-twin\QUARANTINE.md`). Standalone runs pin `MUNIPAL_ROOT=<run>/empty-root`; under pytest the harness monkeypatches `sensing._EMMA_EXTRACTOR` and the e2e test asserts `_corpus_available("healthcare") is False` and `("waste") is False`.
- F16. The Warm Handoff deliverable pack ignores the sector playbook `liability_disclaimers` (`src/munipal/services/deliverable_service.py`): the markdown export carries the pack's own disclaimer block but none of the housing playbook's disclaimer texts (`sector_playbooks.py`). Recorded as an expected `fail` in every golden (`S12.sector_liability_disclaimers_present`, listed in `labkit/report.py::KNOWN_GAP_KEYS`) so a src fix reddens the ratchet.
- F17. Rules-based fallback facts carry `confidence_score` 0.72, below the `min_confidence` of most checklist paths (`src/munipal/services/checklist_service.py::compute_item_status`), so approved facts stay low-confidence and the item stays blocked. Observed in S08 (`low_confidence_paths`); the lab's expectation is mechanical (`required ∩ approved ∩ confident`).

## How a finding moves

- A finding is closed only by a `src/`, `docs/`, `fulfillment/` or deploy change made outside the lab, then a golden re-review (the lab test reddens when BFMS behaviour improves — by design, decision 3 in `governance/decision-request.md`).
- New findings need a path and the run id that observed them. No finding is ever fixed from `lab/`.
