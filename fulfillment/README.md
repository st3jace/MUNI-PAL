# fulfillment/ — what a paying client gets

Product: the **10-Day Obligation Register** (`LS-A2-POST`). Post-close only. We never file, never say "compliant", never pick the controlling clause.

| Item | Where |
|---|---|
| Offer one-pager | `braintrust/workspace/gtm-stack-a-corpus/2026-09-09-OBLIGATION-REGISTER-ONE-PAGER.md` (copy in when frozen) |
| Register template | `register/` (to build) |
| Intake checklist: the documents the client must drop | `intake/` (to build) |
| One-page engagement letter (purpose-built; never letter `06`) | `letter/` (to build) |
| Arthur Q&A config: answers "where is this in my documents" from the client's own docs + reference corpus | `arthur/` (to build) |
| Demo: one synthetic deal from muni-twin, run end to end | `demo/` (to build) |

Build order: demo first. The demo is the proof and the landing page.
