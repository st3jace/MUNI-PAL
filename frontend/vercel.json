{
  "framework": "vite",
  "installCommand": "npm install && ls node_modules/vite/dist/node/cli.js 2>/dev/null || (echo 'vite/dist missing after install — force reinstalling vite' && npm install vite@5 --force)",
  "buildCommand": "npx vite build --config vite.config.sensing.ts",
  "outputDirectory": "dist"
}
